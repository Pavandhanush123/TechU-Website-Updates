// The two — and only two — course delivery modes used across the site. The
// admin CMS editor renders these as the fixed set of selectable mode chips;
// there are intentionally no other modes (no Hybrid / Recorded / etc.).
export const COURSE_MODE_SUGGESTIONS = ["Online", "Offline"] as const;
